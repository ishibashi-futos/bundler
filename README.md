# Homebrew Bundle Applications Dump

## bump command

``` bash
$ brew bundle dump --force --file=.Brewfile
```

## Restore Command

I'm not tried this command
``` bash
$ brew bundle --file=.Brewfile
```

